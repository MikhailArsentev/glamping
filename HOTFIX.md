# 🔥 Быстрое исправление для деплоя

## Проблема

Если вы получили ошибку:
```
[PM2][ERROR] File ecosystem.config.js malformated
ReferenceError: module is not defined in ES module scope
```

## Решение

Файл `ecosystem.config.js` был переименован в `ecosystem.config.cjs` для совместимости с ES модулями.

### На сервере (если уже был деплой)

Выполните на сервере:

```bash
cd /home/glamping/www  # или ваш APP_PATH

# Остановить приложение
pm2 delete glamping 2>/dev/null || true

# Если существует старый файл, удалить
rm -f ecosystem.config.js

# Следующий деплой из GitHub Actions автоматически загрузит правильный файл
```

### Локально (для ручного деплоя)

```bash
# Убедитесь, что используете последнюю версию из репозитория
git pull origin main

# Проверьте наличие ecosystem.config.cjs
ls -la ecosystem.config.cjs

# Теперь деплой должен работать
./scripts/deploy-manual.sh
```

## Что изменилось

- ✅ `ecosystem.config.js` → `ecosystem.config.cjs`
- ✅ Обновлены все GitHub Actions workflows
- ✅ Обновлены скрипты деплоя
- ✅ Обновлена документация

## Проверка после исправления

После следующего деплоя проверьте:

```bash
ssh your-user@your-server
cd /home/glamping/www

# Должен присутствовать файл ecosystem.config.cjs
ls -la ecosystem.config.cjs

# PM2 должен запуститься без ошибок
pm2 start ecosystem.config.cjs --env production

# Проверить статус
pm2 list
pm2 logs glamping --lines 50
```

## Если всё равно не работает

1. Убедитесь, что на сервере установлена правильная версия Node.js:
   ```bash
   node -v  # Должна быть v20.x
   ```

2. Проверьте права доступа:
   ```bash
   ls -la ~/www
   # Владелец должен быть ваш пользователь
   ```

3. Пересоздайте директорию:
   ```bash
   pm2 delete all
   rm -rf ~/www/.output*
   # Сделайте новый деплой через GitHub Actions
   ```

## Следующий деплой

Просто сделайте commit и push - всё будет работать:

```bash
git add .
git commit -m "Fix: update ecosystem config to .cjs extension"
git push origin main
```

GitHub Actions автоматически задеплоит правильную версию! 🚀

