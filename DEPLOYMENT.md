# 🚀 Руководство по деплою сайта Glamping

## Содержание

1. [Подготовка сервера](#подготовка-сервера)
2. [Настройка GitHub Secrets](#настройка-github-secrets)
3. [Автоматический деплой через GitHub Actions](#автоматический-деплой)
4. [Ручной деплой](#ручной-деплой)
5. [Мониторинг и логи](#мониторинг)
6. [Откат к предыдущей версии](#откат)

---

## Подготовка сервера

### 1. Требования к серверу

- **OS:** Ubuntu 20.04/22.04 LTS или Debian 11/12
- **RAM:** Минимум 1GB (рекомендуется 2GB)
- **CPU:** 1 vCPU (рекомендуется 2 vCPU)
- **Диск:** 20GB свободного места
- **Node.js:** v20.x
- **PM2:** для управления процессами
- **Nginx:** в качестве reverse proxy

### 2. Автоматическая настройка сервера

Подключитесь к серверу по SSH и выполните:

```bash
# Скачать скрипт настройки
wget https://raw.githubusercontent.com/your-repo/glamping/main/scripts/setup-server.sh

# Сделать исполняемым
chmod +x setup-server.sh

# Запустить
./setup-server.sh
```

Скрипт автоматически установит:
- Node.js v20
- PM2
- Nginx
- Настроит директории и права доступа

### 3. Ручная настройка сервера (альтернатива)

<details>
<summary>Развернуть инструкцию по ручной настройке</summary>

#### 3.1. Обновление системы

```bash
sudo apt update && sudo apt upgrade -y
```

#### 3.2. Установка Node.js 20

```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs
node -v  # Проверка: должна быть v20.x
```

#### 3.3. Установка PM2

```bash
sudo npm install -g pm2
pm2 startup systemd
```

#### 3.4. Создание директории приложения

```bash
sudo mkdir -p /var/www/glamping
sudo mkdir -p /var/www/glamping/logs
sudo chown -R $USER:$USER /var/www/glamping
```

#### 3.5. Установка и настройка Nginx

```bash
sudo apt install -y nginx

# Создать конфигурацию
sudo nano /etc/nginx/sites-available/glamping
```

Содержимое файла:

```nginx
server {
    listen 80;
    server_name your-domain.com www.your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    location /_nuxt/ {
        proxy_pass http://localhost:3000/_nuxt/;
        proxy_cache_valid 200 60m;
        add_header Cache-Control "public, immutable";
    }
}
```

Активировать конфигурацию:

```bash
sudo ln -s /etc/nginx/sites-available/glamping /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

#### 3.6. Настройка SSL (опционально, но рекомендуется)

```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com -d www.your-domain.com
```

</details>

---

## Настройка GitHub Secrets

Для работы автоматического деплоя необходимо настроить секреты в GitHub.

### 1. Создание SSH ключа для деплоя

На вашем **локальном компьютере**:

```bash
ssh-keygen -t ed25519 -C "github-deploy" -f ~/.ssh/github_deploy
```

### 2. Добавление публичного ключа на сервер

```bash
# Скопировать публичный ключ
cat ~/.ssh/github_deploy.pub

# На сервере добавить в authorized_keys
ssh your-user@your-server
mkdir -p ~/.ssh
nano ~/.ssh/authorized_keys
# Вставить содержимое публичного ключа
chmod 600 ~/.ssh/authorized_keys
```

### 3. Добавление секретов в GitHub

Перейдите в **Settings → Secrets and variables → Actions** вашего репозитория.

Добавьте следующие secrets:

| Название | Описание | Пример |
|----------|----------|--------|
| `SSH_PRIVATE_KEY` | Приватный SSH ключ | Содержимое `~/.ssh/github_deploy` |
| `SERVER_HOST` | IP адрес или домен сервера | `123.45.67.89` или `server.example.com` |
| `SERVER_USER` | Имя пользователя на сервере | `ubuntu` или `deployer` |
| `SERVER_PORT` | SSH порт (опционально) | `22` |
| `APP_PATH` | Путь к приложению на сервере | `/var/www/glamping` |

**Для staging окружения** (опционально):
- `STAGING_SERVER_HOST`
- `STAGING_SERVER_USER`
- `STAGING_SERVER_PORT`
- `STAGING_APP_PATH`

---

## Автоматический деплой

### Production деплой

Автоматический деплой запускается при:
- Push в ветку `main` или `master`
- Ручном запуске через GitHub Actions

#### Процесс деплоя:

1. ✅ Checkout кода из репозитория
2. ✅ Установка Node.js и зависимостей
3. ✅ Сборка приложения (`npm run build`)
4. ✅ Создание архива с .output
5. ✅ Загрузка на сервер через SCP
6. ✅ Бэкап текущей версии
7. ✅ Распаковка новой версии
8. ✅ Перезапуск через PM2
9. ✅ Проверка статуса

### Staging деплой

Деплой в staging окружение при push в ветку `develop` или `staging`.

### Ручной запуск деплоя

1. Перейдите в **Actions** в вашем репозитории
2. Выберите workflow **Deploy to Production**
3. Нажмите **Run workflow**
4. Выберите ветку и нажмите **Run workflow**

---

## Ручной деплой

Если нужно задеплоить вручную без GitHub Actions:

```bash
# Настроить переменные окружения
export SERVER_USER="ubuntu"
export SERVER_HOST="123.45.67.89"
export APP_PATH="/var/www/glamping"

# Сделать скрипт исполняемым
chmod +x scripts/deploy-manual.sh

# Запустить деплой
./scripts/deploy-manual.sh
```

---

## Мониторинг

### Проверка статуса приложения

```bash
ssh your-user@your-server
pm2 list
```

### Просмотр логов

```bash
# Все логи
pm2 logs glamping

# Последние 100 строк
pm2 logs glamping --lines 100

# Только ошибки
pm2 logs glamping --err

# Логи в реальном времени
pm2 logs glamping --raw
```

### Мониторинг ресурсов

```bash
pm2 monit
```

### Метрики производительности

```bash
pm2 show glamping
```

---

## Откат к предыдущей версии

При каждом деплое создается бэкап предыдущей версии.

### Автоматический откат

```bash
ssh your-user@your-server
cd /var/www/glamping

# Остановить приложение
pm2 stop glamping

# Найти последний бэкап
ls -lt | grep .output.backup

# Восстановить бэкап (замените датой вашего бэкапа)
rm -rf .output
mv .output.backup.20241216_210000 .output

# Перезапустить
pm2 restart glamping
```

---

## Troubleshooting

### Проблема: Приложение не запускается

```bash
# Проверить логи
pm2 logs glamping --err

# Проверить конфигурацию
cat ecosystem.config.js

# Перезапустить с нуля
pm2 delete glamping
pm2 start ecosystem.config.js --env production
```

### Проблема: Nginx возвращает 502 Bad Gateway

```bash
# Проверить, что приложение запущено
pm2 list

# Проверить, что порт 3000 слушается
netstat -tulpn | grep 3000

# Проверить логи Nginx
sudo tail -f /var/log/nginx/error.log
```

### Проблема: SSH подключение не работает

```bash
# Проверить формат приватного ключа
cat ~/.ssh/github_deploy

# Ключ должен начинаться с:
# -----BEGIN OPENSSH PRIVATE KEY-----

# Проверить подключение вручную
ssh -i ~/.ssh/github_deploy your-user@your-server
```

---

## Полезные команды PM2

```bash
# Список процессов
pm2 list

# Перезапуск
pm2 restart glamping

# Остановка
pm2 stop glamping

# Удаление
pm2 delete glamping

# Сохранить список процессов
pm2 save

# Обновление PM2
pm2 update

# Очистка логов
pm2 flush
```

---

## Безопасность

### Рекомендации:

1. ✅ Используйте SSH ключи вместо паролей
2. ✅ Настройте firewall (ufw)
3. ✅ Установите SSL сертификат
4. ✅ Ограничьте доступ к SSH (только нужные IP)
5. ✅ Регулярно обновляйте систему
6. ✅ Используйте отдельного пользователя для деплоя (не root)
7. ✅ Настройте автоматическое резервное копирование

---

## Контакты и поддержка

При возникновении проблем:
- Проверьте логи: `pm2 logs glamping`
- Проверьте статус: `pm2 list`
- Проверьте Nginx: `sudo nginx -t`
- Свяжитесь с администратором сервера

**Полезные ссылки:**
- [Документация Nuxt](https://nuxt.com/docs)
- [Документация PM2](https://pm2.keymetrics.io/)
- [Документация Nginx](https://nginx.org/ru/docs/)

