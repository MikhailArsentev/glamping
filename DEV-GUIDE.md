# 🛠️ Руководство по разработке

## Hot Module Replacement (HMR)

Сайт настроен для автоматического обновления при изменении файлов.

### ✅ Что работает автоматически:

- **Vue компоненты** - изменения применяются без перезагрузки страницы
- **Tailwind CSS** - стили обновляются мгновенно
- **Composables** - изменения в логике применяются автоматически
- **Pages** - изменения маршрутов обновляются
- **Layouts** - изменения в layout применяются

### 🚀 Запуск dev сервера:

```bash
# Запустить dev сервер
npm run dev

# Сервер запустится на http://localhost:3000
# HMR работает автоматически
```

### 📝 Рекомендуемые настройки VS Code:

1. **Автосохранение** - уже настроено в `.vscode/settings.json`
   - Файлы сохраняются через 1 секунду после последнего изменения
   
2. **Расширения** (рекомендуется установить):
   - Vue - Official (Volar)
   - Tailwind CSS IntelliSense
   - ESLint
   - Prettier

### 🔍 Как это работает:

1. Вы изменяете файл (например, `components/TheHeader.vue`)
2. Файл автоматически сохраняется (через 1 сек)
3. Vite обнаруживает изменение
4. HMR отправляет обновление в браузер
5. Только изменённый компонент перерисовывается
6. **Состояние приложения сохраняется!**

### 🎯 Примеры:

#### Изменение стилей:
```vue
<template>
  <!-- Измените класс и сразу увидите результат -->
  <div class="bg-blue-500"> <!-- было bg-red-500 -->
    Текст
  </div>
</template>
```

#### Изменение содержимого:
```vue
<template>
  <h1>Новый заголовок</h1> <!-- было "Старый заголовок" -->
</template>
```

#### Изменение логики:
```vue
<script setup>
const count = ref(10) // было ref(0)
// Изменения применятся, но счётчик сбросится
</script>
```

### ⚠️ Когда требуется полная перезагрузка:

Некоторые изменения требуют перезагрузки страницы (Ctrl+R):

1. **Изменения в `nuxt.config.ts`** - перезапустите dev сервер
2. **Новые зависимости** - перезапустите dev сервер
3. **Изменения в `.env` файлах** - перезапустите dev сервер
4. **Новые плагины** - перезапустите dev сервер

### 🐛 Если HMR не работает:

#### Проблема 1: Изменения не применяются

```bash
# Остановите сервер (Ctrl+C)
# Очистите кэш
rm -rf .nuxt node_modules/.vite

# Перезапустите
npm run dev
```

#### Проблема 2: "Connection failed" в консоли

Проверьте что порт HMR не занят:

```bash
# Windows
netstat -ano | findstr "24678"

# Linux/Mac
lsof -i :24678

# Если порт занят, измените в nuxt.config.ts:
# vite.server.hmr.port: 24679
```

#### Проблема 3: Браузер не подключается

1. Откройте DevTools (F12) → Console
2. Ищите ошибки WebSocket
3. Проверьте что не используете VPN/прокси

### 💡 Полезные советы:

#### 1. Включите Vue DevTools:

В `nuxt.config.ts` измените:
```ts
devtools: { enabled: true }
```

Нажмите **Shift + Alt + D** в браузере

#### 2. Используйте компонентную разработку:

Создайте тестовую страницу для разработки компонента:

```vue
<!-- pages/dev/button-test.vue -->
<template>
  <div class="p-8 space-y-4">
    <h1>Тестирование кнопки</h1>
    
    <!-- Тестируйте разные варианты -->
    <Button label="Primary" />
    <Button label="Secondary" outlined />
    <Button label="Large" size="large" />
  </div>
</template>
```

Откройте http://localhost:3000/dev/button-test

#### 3. Быстрая навигация между страницами:

Добавьте в компонент кнопки быстрого доступа:

```vue
<!-- components/DevNavigation.vue -->
<template>
  <div v-if="isDev" class="fixed bottom-4 right-4 space-x-2">
    <NuxtLink to="/">Home</NuxtLink>
    <NuxtLink to="/about">About</NuxtLink>
    <NuxtLink to="/accommodation">Accommodation</NuxtLink>
  </div>
</template>

<script setup>
const isDev = process.dev
</script>
```

### 📊 Мониторинг производительности:

Откройте DevTools → Performance или Lighthouse для проверки:

```bash
# Lighthouse CI (опционально)
npm install -g @lhci/cli
lhci autorun --collect.url=http://localhost:3000
```

### 🔥 Hot Keys в браузере:

- **Ctrl + Shift + R** - жёсткая перезагрузка (игнорировать кэш)
- **F12** - открыть DevTools
- **Ctrl + Shift + I** - открыть DevTools
- **Shift + Alt + D** - открыть Nuxt DevTools

### 📁 Структура для эффективной разработки:

```
components/
├── ui/              # Базовые UI компоненты
│   ├── Button.vue
│   └── Input.vue
├── layout/          # Layout компоненты
│   ├── TheHeader.vue
│   └── TheFooter.vue
└── sections/        # Секции страниц
    ├── HeroSection.vue
    └── ContactSection.vue

pages/
├── index.vue        # Главная
├── about.vue        # О нас
└── dev/             # Страницы для разработки
    ├── components.vue
    └── colors.vue
```

### 🎨 Разработка с Tailwind:

Tailwind автоматически перекомпилируется:

```vue
<template>
  <!-- Добавьте класс и сразу увидите результат -->
  <div class="hover:scale-105 transition-transform">
    Наведите на меня
  </div>
</template>
```

### 🔧 Отладка:

#### Console.log в компонентах:

```vue
<script setup>
const data = ref([])

// Логи будут в браузерной консоли
console.log('Component mounted', data.value)

// Логи серверного рендеринга будут в терминале
if (process.server) {
  console.log('Server-side rendering')
}
</script>
```

#### Vue DevTools:

1. Установите расширение Vue DevTools для Chrome/Firefox
2. Откройте DevTools → Vue tab
3. Инспектируйте компоненты, props, state

### ⚡ Оптимизация скорости разработки:

#### 1. Используйте Component Islands:

```vue
<template>
  <!-- Изолированный компонент, загружается независимо -->
  <NuxtIsland name="HeavyComponent" />
</template>
```

#### 2. Lazy Loading для тяжёлых компонентов:

```vue
<script setup>
const HeavyChart = defineAsyncComponent(() => 
  import('~/components/HeavyChart.vue')
)
</script>
```

### 📱 Тестирование на мобильных:

1. Найдите ваш локальный IP:
   ```bash
   # Windows
   ipconfig
   
   # Linux/Mac
   ifconfig
   ```

2. Откройте на телефоне:
   ```
   http://192.168.x.x:3000
   ```

3. HMR будет работать и на мобильном устройстве!

### 🎯 Быстрые команды:

```bash
# Запуск dev сервера
npm run dev

# Сборка для продакшена (проверить перед деплоем)
npm run build

# Preview продакшен сборки
npm run preview

# Проверка типов TypeScript
npm run typecheck  # если настроено

# Линтинг кода
npm run lint  # если настроено
```

### 🆘 Troubleshooting:

#### "Cannot find module" после добавления зависимости

```bash
# Остановите dev сервер
# Установите зависимости
npm install

# Перезапустите
npm run dev
```

#### Белый экран после изменений

1. Проверьте консоль браузера на ошибки
2. Проверьте терминал на ошибки компиляции
3. Попробуйте Ctrl+R (перезагрузка)
4. Если не помогло - перезапустите dev сервер

#### HMR перестал работать

```bash
# Очистите всё и перезапустите
rm -rf .nuxt node_modules/.vite .output
npm install
npm run dev
```

---

## 🎉 Готово!

Теперь при любых изменениях в компонентах вы будете сразу видеть результат в браузере!

**Приятной разработки!** ✨

